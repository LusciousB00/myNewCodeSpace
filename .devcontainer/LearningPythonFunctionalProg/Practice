#no loops-> use recursion
#no classes-> use high order functions
#no abstract values-> abstract around operations
#don't mutate values-> create copies with the change

def function1():
    x = 1
    def internal():
        return x
    return internal

f1 = function1()
print(f1())
